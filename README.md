# 업무 인수인계 대시보드

학교 담당 업무를 정리해 두고, 다음 담당자에게 **파일 하나로** 넘겨주는 윈도우 프로그램입니다.

받는 분은 Python도, 개발 도구도 설치할 필요가 없습니다. **설치파일 하나면 끝납니다.**

---

## 🚀 그냥 쓰고 싶다면 (대부분 여기만 보면 됩니다)

**만들어진 설치파일이 이미 올라가 있습니다. 내려받아 실행하기만 하면 됩니다.**
이 문서 아래쪽의 명령어들은 **볼 필요 없습니다.** 프로그램을 고칠 때만 쓰는 것들입니다.

### 1단계 — 설치파일 내려받기

👉 **[여기를 눌러 최신 설치파일 받기](https://github.com/sced9120/work-dashboard/releases/latest)**

열린 페이지를 아래로 내리면 **Assets** 라는 목록이 있습니다. 거기서 하나를 고르세요.

| 파일 | 어떤 경우에 |
|---|---|
| `WorkDashboard-Setup-1.1.0.exe` | **보통은 이것.** 설치하면 바탕화면에 아이콘이 생깁니다 |
| `WorkDashboard-Portable-1.1.0.exe` | 학교 PC라 설치가 막혀 있을 때. 설치 없이 그 파일만 더블클릭 |

크기는 약 100MB라 내려받는 데 1~2분 걸릴 수 있습니다.

### 2단계 — 실행하기

받은 파일을 더블클릭합니다.

> **"Windows의 PC 보호" 라는 파란 창이 뜨면?**
> 고장난 게 아닙니다. 개인이 만든 프로그램이라 마이크로소프트에 등록돼 있지 않아서 나오는 안내입니다.
> 창에서 **추가 정보** → **실행** 을 누르면 그대로 진행됩니다.

관리자 권한은 필요 없습니다.

### 3단계 — 쓰기

설치가 끝나면 바탕화면 아이콘을 더블클릭.
이후 사용법은 **[사용안내.md](사용안내.md)** 에 화면별로 정리해 두었습니다.

**여기까지가 전부입니다.** 아래 내용은 프로그램을 수정하거나 새 버전을 만들 때만 필요합니다.

---

## 1.5.0 에서 새로 들어간 것

| 기능 | 설명 |
|---|---|
| **🗄 저장된 문서 학습** | [문서로 업무 만들기]에 모드가 하나 생겼습니다. **이미 보관해 둔 공문을 파일 고르기 없이 그대로 AI에 넘깁니다.** [AI 없이 원문만 보관]으로 잔뜩 쌓아 둔 뒤 나중에 한꺼번에 학습시킬 때 씁니다. **이미 학습한 것은 [학습함]으로 표시**되어 [아직 안 한 것 고르기] 한 번으로 남은 것만 돌릴 수 있고, **요청 횟수와 글자 수를 미리 알려 준 뒤** 시작합니다. 도중에 **[멈추기]** 로 세울 수 있고 그때까지 찾은 것은 남습니다 |

## 1.4.0 에서 새로 들어간 것

| 기능 | 설명 |
|---|---|
| **🧩 워크플로우 그리기** | 업무 흐름을 **상자와 화살표로 그립니다.** 상자를 끌어 옮기고, 두 번 눌러 글자를 고치고, [→ 연결]로 화살표를 잇습니다. 시작·단계·판단·끝 네 가지 상자가 있고, **[✍ 업무로 자동 배치]** 를 누르면 등록된 업무를 시기 순으로 세워 첫 그림을 만들어 줍니다. 그린 것은 **인수인계 파일에 데이터로 담겨** 다음 담당자가 업무별로 열어 봅니다 |
| **달력을 독립 화면으로** | 로드맵 안에 있던 달력을 사이드바 **[🗓 달력]** 으로 빼서 **칸을 크게** 썼습니다. **홈에도 작게 얹혀** 있어 켜자마자 이 달 일정이 보이고, [⛶ 크게] 로 넘어갑니다 |
| **사이드바 정리** | 메뉴가 열두 개가 되어 한 줄로는 찾기 어려웠습니다. **오늘 · 업무 살펴보기 · 자료 만들기 · 관리** 네 묶음으로 나눴습니다 |

## 1.3.1 에서 고친 것

| 고친 것 | 설명 |
|---|---|
| **주제 이름 바꾸기 · 합치기** | 자동으로 뽑은 주제 이름이 어색하면 **[✎ 이름]** 으로 바꿉니다. **두 주제를 같은 이름으로 바꾸면 하나로 합쳐집니다.** 정리해 둔 흐름도도 새 이름으로 함께 옮겨지고, 인수인계 파일에 함께 넘어갑니다 |
| **엉뚱한 주제가 잡히던 문제** | "지도", "학생", "구성원", "1학년", "훈련" 처럼 그것만 봐서는 무슨 일인지 알 수 없는 말이 주제로 뽑히던 것을 고쳤습니다. 낱말 하나짜리 주제는 세 글자 이상만 받고, 공문에 늘 붙는 말(구성원·경비·대상·개최…)은 주제에서 뺐습니다. `급식 지도`·`생활지도`·`대피훈련` 처럼 제대로 묶입니다 |
| **공문 날짜가 2016년으로 잡히던 문제** | 근거 법령("…법률(2016. 12. 20. 개정)")의 연도를 문서 날짜로 잡던 것을 고쳤습니다. **"시행 / 접수 / 기안" 이름표가 붙은 날짜**를 먼저 찾고, 없으면 앞부분 날짜 중 아직 오지 않은 것을 뺀 뒤 **가장 최근 것**을 고릅니다. 그래도 틀리면 **목록에서 날짜를 눌러 직접 고칠 수 있습니다** |

## 1.3.0 에서 새로 들어간 것

| 기능 | 설명 |
|---|---|
| **업무별 보기** | 왼쪽에서 업무 주제를 고르면 **그 업무의 공문이 날짜순으로**, 진행 업무가 학사 순서로 늘어섭니다. 공문 제목을 누르면 원문이 그 자리에서 펼쳐집니다 |
| **업무 흐름도** | 주제마다 **[🗺 업무 흐름도]** — 등록된 업무·절차·유의사항으로 **초안을 자동으로 짜 주고**, 고쳐서 저장하면 **인수인계 파일에 함께** 넘어갑니다. AI 없이 있는 자료만 씁니다 |
| **인포그래픽 개편** | 업무를 낱낱이 늘어놓던 것을 **주마다 주제로 묶어** 보여 줍니다. 제목이 400개라도 주제 10여 개로 정리됩니다. 주제를 누르면 업무별 보기로 넘어갑니다 |
| **보기 차례 변경** | **인포그래픽**이 맨 앞이자 기본이 되고, 인포그래픽 · 업무별 · 달력 · 목록 순서가 됐습니다. 낱낱의 업무는 [목록]에서 봅니다 |

## 1.2.0 에서 새로 들어간 것

| 기능 | 설명 |
|---|---|
| **로드맵 — 달력 보기** | 핸드폰 달력처럼 **날짜에 일정을 직접 넣습니다.** 날짜를 두 번 누르면 아래에서 입력창이 올라오고, 제목·기간·시각·색·메모를 넣습니다. **[기한으로 챙기기]** 를 켜면 D-day 가 붙고 윈도우 알림 대상이 됩니다. [절차 기한]에 넣어 둔 것도 달력에 ⏰ 로 함께 보입니다 |
| **로드맵 — 인포그래픽 보기** | 3월부터 이듬해 2월까지를 **하나의 흐름도**로 그립니다. 완료율 고리, 학기별 건수, 가장 바쁜 달, 달마다의 학사 흐름과 진행 막대가 함께 나옵니다. 인수인계 때 "한 해가 이렇게 돌아갑니다" 를 보여 주기 좋습니다 |

## 1.1.2 에서 새로 들어간 것

| 기능 | 설명 |
|---|---|
| **기본 모델 목록 갱신** | OpenAI 는 `gpt-5.6-terra`·`gpt-5.6-luna`, Gemini 는 `gemini-3.6-flash`·`gemini-3.5-flash-lite` 로 바꿨습니다 |
| **기본 모델도 삭제 가능** | 목록의 **모든** 모델을 × 로 뺄 수 있습니다. 기본 제공 모델을 뺐다면 **[↺ 기본 목록으로 되돌리기]** 로 언제든 복구됩니다 |
| **반응 속도 개선** | 모델을 고를 때마다 레지스트리 쓰기·트레이 재생성·설정 파일 4회 읽기가 함께 돌던 것을 없앴습니다. 공문을 여러 건 보관할 때 건마다 DB 전체를 다시 쓰던 것도 한 번으로 묶어 **338건 기준 4.9초 → 0.4초** 가 됐습니다. 켤 때도 바뀐 것이 없으면 DB 를 다시 쓰지 않습니다 |

## 1.1.1 에서 새로 들어간 것

| 기능 | 설명 |
|---|---|
| **업무 도우미 (AI 챗봇)** | 보관해 둔 공문·업무·일지를 근거로 대화하듯 묻고 답합니다. 질문과 관련된 자료를 자동으로 찾아 함께 넣고, 답 아래에 **근거 자료 이름**을 보여 줍니다. 없는 내용은 지어내지 않고 "보관 자료에는 없다"고 밝힙니다 |
| **Claude(Anthropic) 지원** | 설정의 [사용할 서비스]에 **Claude** 가 추가됐습니다. Gemini·OpenAI 와 같은 방식으로 키만 넣으면 됩니다. 기본 모델은 `claude-sonnet-5` (설정에서 `claude-opus-5`·`claude-haiku-4-5` 로 바꿀 수 있습니다) |
| **OpenAI 연결 오류 수정** | OpenAI 로 문서 분석·연결 테스트를 할 때 `'messages' must contain the word 'json'` (400) 오류가 나던 문제를 고쳤습니다. JSON 형식을 요구하는 요청에 항상 그 조건을 채워 보냅니다 |
| **OpenAI temperature 오류 대응** | `gpt-5.x`·`o1`·`o3`·`o4` 처럼 temperature 를 고정값만 받는 모델을 넣어도 자동으로 인식해 파라미터를 뺍니다. 미리 몰랐던 모델은 400 을 만난 순간 한 번만 재시도해서 통과합니다 |
| **기능별 모델 선택** | 문서 분석·검색 요약·대본/회의록·업무 도우미 각 화면에서 **어떤 서비스·모델을 쓸지 그때그때 고를 수 있습니다.** 등록된 API 키가 있는 서비스만 골라집니다. 선택은 저장돼 다음에도 이어집니다 |
| **모델 목록 직접 관리** | [설정]에서 **모델 이름을 목록에 추가·삭제**할 수 있습니다. 기본 제공 모델까지 뺄 수 있어(→ [↺ 기본 목록으로 되돌리기]로 복구) 실제로 쓰는 것만 남길 수 있습니다. 목록은 모든 AI 화면의 모델 고르개에 그대로 나오므로, 새 모델이 나와도 프로그램을 새로 받을 필요 없이 이름만 넣으면 됩니다 |
| **자료 폴더 안내 수정** | 자료·백업·설정이 실제로 저장되는 폴더를 `%APPDATA%\work-dashboard\` 로 바로잡았습니다 (예전 문서는 폴더 이름을 잘못 적어 두었습니다) |

## 1.1.0 에서 새로 들어간 것

| 기능 | 설명 |
|---|---|
| **통합 검색** | 올린 공문의 **원문을 그대로 보관**하고, 등록된 업무와 함께 검색합니다. 1.0.0 까지는 AI가 뽑아낸 업무만 남고 원문은 버려졌습니다 |
| **AI 경과 정리** | 검색 결과만 근거로 "그동안 이런 일이 있었다"를 시간순으로 정리 |
| **선도위원회 대본·회의록** | 기존 대본을 본보기로 넣어 두면 그 형식대로 새 사안의 초안 생성 |
| **가명처리** | 학생 실명을 전송 직전 `학생A` 로 바꾸고 결과에서 되돌립니다. 주민번호·연락처는 자동 삭제, 가리기 실패 시 전송 중단 |
| **서식 채우기** | `{{학생명}}` 자리를 채우는 방식. **인터넷을 쓰지 않아** 개인정보가 나가지 않습니다 |
| **절차 기한** | 통보·통지 기한을 D-day 로 관리. 개인정보 우려로 인수인계 파일에서 기본 제외 |
| **업데이트 알림** | 켤 때 새 버전을 확인해 알려 줍니다. 설치형·Portable 모두 동작하고, 자료는 그대로 남습니다 |
| **자동 설치** | 설치형이면 배너에서 클릭 한 번으로 받아 설치합니다. Portable 은 안내만 하고 페이지를 열어 줍니다 |
| **기한 알림 · 트레이 · 자동 실행** | 기한이 다가오면 윈도우 알림. 기본은 모두 꺼져 있고 이 PC에만 저장됩니다 |
| **업무 일지** | 날짜별 기록. 통합 검색에서 함께 찾아집니다 |

## 예전 Streamlit 버전과의 차이

예전 Streamlit 버전(`my_dashboard/app.py`)을 Electron 데스크톱 앱으로 새로 만든 것입니다.

| | 예전 (Streamlit) | 지금 (Electron) |
|---|---|---|
| 설치 | Python + PyCharm + `pip install` 7개 | 설치파일 실행 한 번 |
| 실행 | 명령 프롬프트에 명령어 입력 | 바탕화면 아이콘 더블클릭 |
| 한글(.hwp) 읽기 | 압축을 풀지 않아 대부분 깨짐 (한글 프로그램 설치 시에만 정상) | 압축 해제 후 직접 해석, 한글 미설치 PC에서도 동작 |
| 상세 가이드 편집 | 화면에서 고쳐도 저장 안 됨 | 저장됨 |
| AI 모델 | 존재하지 않는 이름(`GPT 5.2`, `gemini-3.0-pro`) → 호출 실패 | 실제 모델명, 설정에서 직접 입력 가능 |
| API 키 | 인수인계 DB 안에 평문 저장 → 남에게 그대로 전달됨 | OS 보안 저장소에 이 PC에만 저장, 인수인계 파일에서 자동 제거 |
| AI 분석 결과 | 확인 없이 바로 DB에 등록 | 목록으로 보여주고 고른 것만 등록 |
| 긴 문서 | 앞 35,000자만 분석 | 여러 조각으로 나눠 전부 분석 |

---

# 🔧 프로그램을 고치거나 새 버전을 만들 때 (개발자용)

여기부터는 **코드를 수정할 때만** 필요합니다. 그냥 쓰실 거면 위 3단계로 충분합니다.

## 명령어는 어디에 붙여넣나요?

이 아래 나오는 회색 상자들은 전부 **터미널(명령 창)** 에 입력하는 것입니다.
"어디에 붙여넣지?" 싶으셨다면 아래 순서대로 하시면 됩니다.

### 윈도우에서 터미널 여는 법

1. 키보드에서 `Windows 키` 를 누릅니다
2. `powershell` 이라고 칩니다
3. **Windows PowerShell** 을 엽니다 → 검고 파란 창이 뜹니다

### 중요 — 반드시 "코드가 있는 폴더"로 먼저 이동해야 합니다

터미널을 갓 열면 엉뚱한 위치(예: `C:\Users\내이름`)에 있습니다.
그 상태로 명령어를 붙여넣으면 "그런 파일 없다"는 오류가 납니다.

코드를 내려받고 그 폴더로 이동하는 것까지 한 번에:

```powershell
cd $HOME\Desktop
git clone https://github.com/sced9120/work-dashboard.git
cd work-dashboard
```

이제 터미널 왼쪽에 `...\Desktop\work-dashboard>` 라고 보이면 준비 끝입니다.
**앞으로 나오는 모든 명령어는 이 창에, 이 위치에서** 실행합니다.

> 붙여넣기는 터미널 안에서 **마우스 오른쪽 클릭** 또는 `Ctrl+V` 입니다.
> 명령어 한 줄을 붙여넣은 뒤에는 꼭 **Enter** 를 눌러야 실행됩니다.

## 필요한 도구

이 PC에 아직 없다면 먼저 설치하세요.

| 도구 | 받는 곳 | 확인 명령 |
|---|---|---|
| Git | [git-scm.com](https://git-scm.com/download/win) | `git --version` |
| Node.js (LTS) | [nodejs.org](https://nodejs.org) | `node --version` |

확인 명령을 쳐서 버전 번호가 나오면 설치된 것입니다.
설치 직후에는 **터미널을 닫았다가 다시 열어야** 인식됩니다.

## 개발 모드로 실행

```powershell
npm install
```

```powershell
npm run dev
```

`npm install` 은 처음 한 번만 하면 되고, 몇 분 걸립니다.

## 타입 검사

```powershell
npm run typecheck
```

## 새 설치파일 만들기 (GitHub Actions)

윈도우 설치파일은 윈도우에서만 만들 수 있습니다.
맥에서 작업 중이라면 GitHub의 윈도우 서버에서 대신 빌드하면 됩니다.

1. GitHub 저장소 → **Actions** 탭
2. 왼쪽 목록에서 **윈도우 설치파일 빌드**
3. 오른쪽 **Run workflow** 버튼 클릭
4. 5분쯤 뒤 완료 → 그 실행을 눌러 맨 아래 **Artifacts** 에서 `WorkDashboard-Windows` 내려받기

## 버전 올려 배포하기

`package.json` 의 `version` 을 올린 뒤, 태그를 붙여 올리면
설치파일이 자동으로 **Releases** 에 첨부되어 링크로 바로 공유됩니다.

```powershell
git add -A
git commit -m "v1.0.1"
git push
git tag v1.0.1
git push origin v1.0.1
```

## 맥에서 로컬 빌드 (dmg)

```bash
npm run build:mac
```

---

## 구조

```
electron/
  main/
    index.ts      창 생성, IPC 핸들러 등록
    db.ts         SQLite(sql.js) 자료 저장 — 예전 .db 파일과 호환
    secrets.ts    API 키를 OS 보안 저장소에 보관 (인수인계 파일과 분리)
    ai.ts         OpenAI / Gemini 호출, 긴 문서 분할, 응답 검증
    extract/
      index.ts    PDF · 엑셀 · 워드 · 텍스트에서 글자 뽑기
      hwp.ts      한글 파일(.hwp 5.0 / .hwpx) 해석
  preload/
    index.ts      렌더러에 노출할 API 정의
src/
  App.tsx         화면 전환
  pages/          홈 · 로드맵 · 상세가이드 · 문서학습 · 데이터 · 설정
shared/types.ts   양쪽이 함께 쓰는 타입
scripts/make-icon.mjs  아이콘 생성 (외부 도구 없이 픽셀을 직접 그림)
```

## 자료가 저장되는 곳

| | 경로 |
|---|---|
| 업무·공지 자료 | `%APPDATA%\work-dashboard\work-dashboard.db` |
| 자동 백업 | `%APPDATA%\work-dashboard\backups\` |
| API 키 | `%APPDATA%\work-dashboard\settings.json` (Windows DPAPI로 암호화) |

> 경로를 여는 법: `Windows 키 + R` → `%APPDATA%\work-dashboard` 입력 → Enter

자료 파일은 평범한 SQLite 파일이라 필요하면 DB 뷰어로도 열립니다.
예전 Streamlit 버전에서 쓰던 `school_admin_v25_final.db` 도 그대로 불러올 수 있습니다.
